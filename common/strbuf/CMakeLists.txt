set(TARGET_NAME STRBUF)
add_library(${TARGET_NAME} INTERFACE)

target_sources(${TARGET_NAME} INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/strbuf.c")

target_include_directories(${TARGET_NAME} INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
