# !/bin/bash

# PER Build runner script, basically just a wrapper for CMake and Make

cd /per                         # Make sure we are in the root directory of our project
cmake -S . -B build -G Ninja "${@:1}"    # Invoke CMake
cd build
ninja                           # Make our target